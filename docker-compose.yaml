version: '3.3'

services:
  redis:
    image: redis
    command: ["redis-server"]
    ports:
      - "6379:6379"

  backend:
    container_name: backendDjangoProject
    environment:
      PYTHONUNBUFFERED: 1
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app/back
    ports:
      - "8000:8000"
    depends_on:
      - redis

  celery-worker:
    build: .
    command: ["celery", "-A", "config", "worker", "-l", "info"]
    volumes:
      - .:/app/back
    depends_on:
      - redis
      - backend
